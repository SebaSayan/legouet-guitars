import React from 'react';
import Navbar from './Navbar';

const Header = () => {
    return (
        <header>
            <Navbar />
            <div className="logo"></div>
        </header>
    );
};

export default Header;